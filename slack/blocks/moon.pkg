// Block Kit is also what an event receiver parses, so it does not live inside
// `client`. Its fixtures are small enough to embed as raw strings, which keeps
// this package free of filesystem access and therefore portable.
import {
  "marianoguerra/slack/internal/jsonx",
  "moonbitlang/core/json",
}

import {
  "moonbitlang/core/test",
  "moonbitlang/core/json",
} for "test"
